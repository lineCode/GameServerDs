#ifndef MINIPROJECT_GAME_TYPEDEF_H
#define MINIPROJECT_GAME_TYPEDEF_H

#include <string>

namespace MiniProject
{
    typedef long long RoomId;
    typedef uint32_t UserId;
    typedef long long RoleId;
    typedef long long LevelId;
    typedef long long PlayerId;
    typedef unsigned long long Time;

} // namespace MiniProject

#endif