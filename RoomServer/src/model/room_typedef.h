#ifndef MINIPROJECT_ROOM_TYPEDEF_H
#define MINIPROJECT_ROOM_TYPEDEF_H

#include <string>

namespace MiniProject
{
    typedef unsigned long long RoomId;
    typedef uint64_t UserId;
    typedef long long RoleId;
    typedef long long LevelId;
    typedef long long PlayerId;
    typedef std::string UserName;

} // namespace MiniProject

#endif