#ifndef MINIPROJECT_BATTLESERVER_TYPEDEF_H
#define MINIPROJECT_BATTLESERVER_TYPEDEF_H

#include <string>

namespace MiniProject
{
    typedef long long RoomId;
    typedef uint32_t UserId;
    typedef long long RoleId;
    // typedef unsigned long long NetId;
    typedef int LevelId;
    typedef long long PlayerId;
    typedef unsigned long long GameId;

} // namespace MiniProject

#endif